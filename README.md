# Personal Landing

Одностраничный сайт веб-разработчика из Коломны.

## Структура

- `index.html` — разметка страницы;
- `css/style.css` — стили;
- `js/main.js` — интерактивность;
- `assets/images` — фотографии и изображения;
- `assets/icons` — иконки;
- `assets/fonts` — локальные шрифты.
